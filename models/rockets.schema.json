{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "name": { "type": "string" },
      "slug": { "type": "string" },
      "manufacturer": { "type": "string" },
      "first_flight": { "type": "integer", "default": 0 },
      "status": { "type": "string", "enum": ["Retired", "Active", "Planned"], "default": "Planned" },
      "price": { "type": "integer", "default": 0 },
      "liftoff_thrust": { "type": "integer", "default": 0 },
      "payload_LEO": { "type": "integer", "default": 0 },
      "payload_GTO": { "type": "integer", "default": 0 },
      "stages": { "type": "integer", "default": 0 },
      "strap_ons": { "type": "integer", "default": 0 },
      "rocket_height": { "type": "number", "default": 0 },
      "fairing_diameter": { "type": "number", "default": 0 },
      "fairing_height": { "type": "number", "default": 0 },
      "variants": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "name": { "type": "string" },
            "slug": { "type": "string" },
            "first_flight": { "type": "integer", "default": 0 },
            "status": { "type": "string", "enum": ["Retired", "Active", "Planned"], "default": "Planned" },
            "price": { "type": "integer", "default": 0 },
            "liftoff_thrust": { "type": "integer", "default": 0 },
            "payload_LEO": { "type": "integer", "default": 0 },
            "payload_GTO": { "type": "integer", "default": 0 },
            "stages": { "type": "integer", "default": 0 },
            "strap_ons": { "type": "integer", "default": 0 },
            "rocket_height": { "type": "number", "default": 0 },
            "fairing_diameter": { "type": "number", "default": 0 },
            "fairing_height": { "type": "number", "default": 0 }
          },
          "required": ["name", "slug"],
          "additionalProperties": false
        },
        "default": []
      }
    },
    "required": ["name", "slug", "manufacturer"],
    "additionalProperties": false
  }
}
