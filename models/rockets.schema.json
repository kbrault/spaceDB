{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "array",
  "items": {
    "type": "object",
    "properties": {
      "name": { "type": "string" },
      "slug": { "type": "string" },
      "agency": { "type": "string" },
      "first_flight": { "type": "integer" },
      "variants": {
        "type": "array",
        "items": {
          "type": "object",
          "properties": {
            "name": { "type": "string" },
            "slug": { "type": "string" },
            "first_flight": { "type": "integer" }
          },
          "required": ["name", "slug", "first_flight"],
          "additionalProperties": false
        }
      }
    },
    "required": ["name", "slug", "agency", "first_flight"],
    "additionalProperties": false
  }
}
